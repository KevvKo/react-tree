const nodeModifier = () => {


    mapToTree: () => {
        let tree = {}
        return tree;
    }

    /**
     * @params (Object) data
     * @returns (treeNode)
     */
    mapToTreeNode: ( dataObject ) => {
        let treeNode = {};
        return treeNode;
    }

    traverse: () => {
        
    }
}

export default nodeModifier;