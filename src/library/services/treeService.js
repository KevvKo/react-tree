
const treeModifier = () => {


    mapToTree: () => {
        let tree = {};
        return tree;
    };

    /**
     * @params (Object) data
     * @returns (treeNode)
     */
    mapToTreeNode: ( dataObject ) => {
        let treeNode = {};
        return treeNode;
    };

    traverse: () => {
        
    };
};

const branchModifier = () => {

};

const leafModifier = () => {

};